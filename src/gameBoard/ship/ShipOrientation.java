package gameBoard.ship;

import UI.Command;
import UI.IClickable;

public enum ShipOrientation implements IClickable {
    VERTICAL, HORIZONTAL;

	@Override
	public void onClick(Command targetCommand) {
		// TODO Auto-generated method stub
		
	}
}
